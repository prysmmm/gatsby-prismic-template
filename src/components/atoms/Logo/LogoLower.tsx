import React from 'react';
import { createIcon } from '@chakra-ui/icons';
import { motion } from 'framer-motion';

export const LogoLower = createIcon({
  displayName: `Logo`,
  viewBox: `0 0 764 228`,
  path: <div></div>,
});

export const MotionLogoLower = motion(LogoLower);
